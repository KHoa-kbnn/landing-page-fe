- hosts: web1
  become: yes
  tasks:
    - name: Cai goi nginx
      apt:
        name: nginx
        state: present
        update_cache: yes

    - name: Copy file cau hinh tu template
      template:
        src: templates/nginx.conf.j2
        dest: /etc/nginx/conf.d/default.conf
      notify: Restart nginx

    - name: Copy ma nguon frontend vao thu muc web
      copy:
        src: ../dist/
        dest: /var/www/html/
        owner: www-data
        group: www-data
        mode: '0755'

  handlers:
    - name: Restart nginx
      service:
        name: nginx
        state: restarted